packages:
  # Server machine
  server:
  
  # Workstation machine
  system:
    manjaro:
      pacman:
      - 'zsh'
      - 'fzf'
      - 'docker'
      - 'docker-compose'
  
  # Devpod container
  devpod:
    debian:
      apt:
      - 'zsh'

